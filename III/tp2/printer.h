#include "structs.h"

#ifndef PRINTER
#define PRINTER

// ----- HELPER FUNCTIONS (used only in development)

void print_point_with2(Point *point, char *str1, char *str2);
void print_point_with(Point *point, char *str);
void print_point(Point *point);
void print_map(Map *map);

#endif
